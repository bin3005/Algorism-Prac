import java.util.ArrayList;
import java.util.Collections;
import java.util.Scanner;

public class Greedy1202 {

	public static void main(String[] args) {
		Scanner in = new Scanner(System.in);
		int cubeNum = in.nextInt();
		int bagNum = in.nextInt();
		ArrayList<Cube> cubeList = new ArrayList<>();
		ArrayList<Integer> bagList = new ArrayList<Integer>();
		int sum = 0 ;
		
		for(int i = 0 ; i < cubeNum ; i++) {
			cubeList.add(new Cube(in.nextInt(),in.nextInt()));
		}
		for(int i = 0 ; i < bagNum ; i++) {
			bagList.add(in.nextInt());
		}
		Collections.sort(cubeList);
		Collections.sort(bagList);
		
		while(bagList.size() >= 1) {
			for(int i = 0 ; i < bagList.size(); i++) {
				for(int j = 0 ; j < cubeList.size() ; j++) {
					if(bagList.get(i) <= cubeList.get(i).weight  ) {
						System.out.println(sum);
						sum += cubeList.get(i).price;
						bagList.remove(i);
						cubeList.remove(i);
						break;
					}
					
				}
			}
		}
		
		System.out.println(sum);
	}

}

class Cube implements Comparable<Cube>{
	int weight;
	int price;
	public Cube() {
		super();
	}
	public Cube(int weight, int price) {
		super();
		this.weight = weight;
		this.price = price;
	}
	@Override
	public int compareTo(Cube cube) {

		if(price < cube.price) {
			return 1;
		}else {
			return -1;
		}
	}
	@Override
	public String toString() {
		return "Cube [weight=" + weight + ", price=" + price + "]";
	}
	
	
	
	
	
}
